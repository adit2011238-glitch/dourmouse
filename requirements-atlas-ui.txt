streamlit>=1.50  # NOT INSTALLED in .venv (pip show confirms absent) — left floating rather than fabricating a pin
plotly>=6.0  # NOT INSTALLED in .venv (pip show confirms absent) — left floating rather than fabricating a pin
pandas==3.0.5
requests==2.34.2
python-dotenv==1.2.2
