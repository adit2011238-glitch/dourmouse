# Desktop app extra (v2.5) — PyWebView gives DOURMOUSE its native macOS window.
# Install with the base requirements: pip install -r requirements.txt -r requirements-desktop.txt
pywebview==6.2.1
pystray==0.19.5  # Vision stage 3 — system tray + camera/mic kill-switch (dourmouse/tray.py); pulls in Pillow for the tray icon bitmap
